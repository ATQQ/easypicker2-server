import { BOOLEAN } from './public'

export interface People {
    id?: number
    task_key?: number
    taskKey?: number
    user_id?: number
    userId?: number
    name?: string
    status?: BOOLEAN
    submit_date?: Date
    submitDate?: Date
    submit_count?: number
    submitCount?: number
}